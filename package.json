{
  "name": "yt-focus",
  "version": "1.0.7",
  "description": "Hide YouTube Shorts & Playables, plus dual subtitles to learn a language while you watch. Focus + fluency, free.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "compile": "wxt build --debug && tsc --noEmit",
    "zip": "wxt zip"
  },
  "devDependencies": {
    "wxt": "^0.20.0",
    "typescript": "^5.5.0",
    "@types/chrome": "^0.0.270"
  }
}
