{
  "name": "yt-focus",
  "version": "1.0.4",
  "description": "Focus on long-form YouTube and learn from it: hide Shorts and Playables, and show a secondary subtitle in the language you are learning.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "compile": "wxt build --debug && tsc --noEmit",
    "zip": "wxt zip"
  },
  "devDependencies": {
    "wxt": "^0.20.0",
    "typescript": "^5.5.0",
    "@types/chrome": "^0.0.270"
  }
}
